The test suite resources are structured under following folders.
  1. lib: contain all library dependencies including framework jasmine
  2. spec: contain the test specifications written.
  3. src: contain all source code files that are been tested.
  4. SpecRunner.html : contain resource references and fixture content used to perform tests.

  To run the test suite, launch SpecRunner.html on your default browser. 