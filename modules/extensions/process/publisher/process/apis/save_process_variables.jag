<%
importPackage(org.wso2.carbon.pc.core);

var log = new Log();
if(log.isDebugEnabled()){
    log.debug("Process Varibales Adding Logic");
}

var ps = new ProcessStore();
var variableDetails = String(request.getParameter("processVariablesInfo"));
//var variableDetails = String(request.getContent());
var status = ps.saveProcessVariables(variableDetails);
print(status);
%>