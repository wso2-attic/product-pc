<%


var log = new Log();
if(log.isDebugEnabled()){
    log.debug("INSIDE get process list...");
}

log.info("fetching the process list from the registry...");
var ps = new Packages.org.wso2.carbon.processCenter.core.ProcessStore;

var processList = ps.getProcesses();
log.info(processList);
if(log.isDebugEnabled()){
    log.debug(processList);
}
print(processList);
%>
